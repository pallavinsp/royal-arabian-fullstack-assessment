import type { Package } from "@/types";
import { PackageCard } from "./PackageCard";
export function PackageGrid({ packages }: { packages: Package[] }) { return <section id="packages" className="section-pad scroll-mt-12 bg-white"><div className="container-ra"><div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="eyebrow">Curated journeys</p><h2 className="heading mt-4">Our China packages</h2></div><p className="max-w-md text-sm leading-6 text-slate-500">Thoughtfully designed private and small-group journeys, shaped by local insight and refined by our travel specialists.</p></div>{packages.length ? <div className="mt-12 grid gap-7 md:grid-cols-2 lg:grid-cols-3">{packages.map((item) => <PackageCard key={item._id} item={item} />)}</div> : <p className="mt-12 rounded-sm border border-slate-200 bg-slate-50 p-8 text-slate-600">Our travel specialists are preparing new China journeys. Please contact us for a bespoke itinerary.</p>}</div></section>; }
