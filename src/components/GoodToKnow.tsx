import type { GoodToKnow as GoodToKnowItem } from "@/types";
export function GoodToKnow({ items }: { items: GoodToKnowItem[] }) { if (!items?.length) return null; return <section id="good-to-know" className="section-pad scroll-mt-12 bg-ra-navy text-white"><div className="container-ra grid gap-12 lg:grid-cols-[.7fr_1.3fr]"><div><p className="eyebrow text-ra-gold">Before you go</p><h2 className="heading mt-4 text-white">Good to know</h2><p className="mt-5 max-w-sm leading-7 text-white/60">A few essentials to help you arrive informed and travel with confidence.</p></div><dl className="grid gap-x-10 sm:grid-cols-2">{items.map((item, index) => <div key={item._key} className="border-t border-white/15 py-7"><dt className="flex items-center gap-3 font-serif text-xl"><span className="text-xs text-ra-gold">0{index + 1}</span>{item.title}</dt><dd className="mt-3 text-sm leading-6 text-white/65">{item.content}</dd></div>)}</dl></div></section>; }
