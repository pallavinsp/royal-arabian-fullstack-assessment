import Link from "next/link";
import type { Package } from "@/types";
import { formatPrice } from "@/lib/helpers";
import { SanityImage } from "./SanityImage";
import { ArrowIcon, ClockIcon } from "./icons";
export function PackageCard({ item }: { item: Package }) { return <article className="group overflow-hidden rounded-sm bg-white shadow-[0_12px_40px_rgba(15,35,55,.08)] transition duration-300 hover:-translate-y-2 hover:shadow-[0_20px_55px_rgba(15,35,55,.16)]"><Link href={`/cn/packages/${item.slug}`} className="block focus:outline-none focus-visible:ring-2 focus-visible:ring-ra-gold"><div className="relative aspect-[4/3] overflow-hidden bg-slate-200"><SanityImage value={item.heroImage} fill sizes="(min-width: 1024px) 33vw, (min-width: 640px) 50vw, 100vw" className="object-cover transition duration-700 group-hover:scale-105" />{item.featured && <span className="absolute left-5 top-5 bg-ra-gold px-3 py-1.5 text-[10px] font-bold uppercase tracking-[.18em] text-ra-navy">Featured</span>}</div><div className="p-6"><div className="flex items-center gap-2 text-xs font-semibold uppercase tracking-[.12em] text-slate-500"><ClockIcon className="size-4 text-ra-orange" />{item.duration}</div><h3 className="mt-4 font-serif text-2xl leading-tight text-ra-navy transition group-hover:text-ra-orange">{item.name}</h3><p className="mt-3 line-clamp-3 min-h-[4.5rem] text-sm leading-6 text-slate-600">{item.shortDescription}</p><div className="mt-6 flex items-end justify-between border-t border-slate-100 pt-5"><div><span className="block text-[10px] uppercase tracking-wider text-slate-400">From</span><strong className="font-serif text-xl text-ra-navy">{formatPrice(item.price)}</strong>{item.originalPrice && <span className="ml-2 text-xs text-slate-400 line-through">{formatPrice(item.originalPrice)}</span>}</div><span className="grid size-10 place-items-center rounded-full border border-slate-200 text-ra-navy transition group-hover:border-ra-gold group-hover:bg-ra-gold"><ArrowIcon className="size-4" /></span></div></div></Link></article>; }
